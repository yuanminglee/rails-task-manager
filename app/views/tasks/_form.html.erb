<%= simple_form_for(task) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <%= f.check_box :completed unless task.new_record? %>
  <%= f.label :completed unless task.new_record? %>
  <%= f.submit %>
<% end %>

<%= link_to "Back to tasks", tasks_path %>
