<%= simple_form_for(task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <%= f.input :completed unless task.new_record? %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>

<%= link_to "Back to tasks", tasks_path %>
